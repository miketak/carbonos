/**
 * User accounts and authentication. Owns the {@code users} table, session
 * login, and the admin-facing user CRUD API. Public API: {@link
 * com.carbonos.user.UserCreated} (domain event).
 */
package com.carbonos.user;
