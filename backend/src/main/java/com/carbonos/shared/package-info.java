/**
 * Shared, cross-cutting concerns available to all application modules:
 * web configuration, error handling, and common infrastructure.
 * <p>
 * Business logic never lives here. Each business capability gets its own
 * top-level package under {@code com.carbonos} (a Spring Modulith module).
 */
package com.carbonos.shared;
