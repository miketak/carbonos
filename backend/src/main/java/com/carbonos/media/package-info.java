/**
 * Object storage for media files, backed by any S3-compatible store (MinIO
 * locally, Cloudflare R2 or a Railway bucket in production). Public API:
 * {@link com.carbonos.media.MediaStorage}.
 */
package com.carbonos.media;
