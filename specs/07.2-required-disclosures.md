# 07.2: Required disclosures

- **Status**: Implemented
- **Protocol**: Chapter 9 (required information), Chapter 4 (scope 2 covers
  electricity, steam, heat, and cooling), the 2013 amendment (gases in
  tonnes, the source of the GWP values), the Scope 2 Guidance Chapter 7
  (Scope 2 Quality Criteria, residual mix, the method behind a single total)
- **Owner**: Michael Takrama
- **Created**: 2026-09-08 (conformance review); implemented 2026-09-08
- **Modules**: `ghg` (`ActivityCategory`, `EmissionFactor`, `MarketFactor`,
  boundary treatments, the report endpoint), `src/features/ghg` (report page,
  market-factor card, boundary section)

## Problem

Spec 07.1 made the report carry the Chapter 9 elements. The review of
2026-09-08 found the following stated conditions unmet.

1. **Units and gas masses.** The amendment requires "emissions data for all
   seven GHGs separately in metric tonnes and in tonnes of CO2 equivalent".
   The report is in kilograms, and HFCs and PFCs appear as CO2e only because
   the factors carry blends as CO2e.
2. **The source of the potentials.** The amendment requires the report to
   "report the source of the GWP values and indicate if multiple Assessment
   Reports have been used". The methodology statement names the inventory's
   set and says that blends keep the source's potentials, but not which
   assessment report the source used.
3. **Cooling.** Chapter 4 defines scope 2 to cover electricity, steam,
   heating, and cooling. There is no category for purchased cooling.
4. **Scope 2 Quality Criteria.** The Guidance: "Companies shall ensure that
   any contractual instruments used in the market-based method total meet the
   Scope 2 Quality Criteria ... If instruments do not meet the Criteria, then
   other data shall be used as an alternative". Nothing records whether an
   instrument meets them.
5. **Residual mix.** "If a residual mix is not currently available, reporters
   shall note that an adjusted emissions factor is not available or has not
   been estimated to account for voluntary purchases and this may result in
   double counting between electricity consumers." Nothing records or prints
   this.
6. **The method behind a single total.** "If reporting a single corporate
   inventory total ... companies shall disclose which method was chosen." The
   run total uses the location-based figure and the report does not say so.
   The Guidance also asks for the method used for the base year's scope 2 and
   whether historic location-based data stands as a proxy for market-based.
7. **Excluded operations.** Chapter 9 requires "any specific exclusions of
   sources, facilities, and/or operations". The report lists excluded records
   and zero-share entities, but an entity or facility of the organization that
   the accountant simply did not tick into the boundary is not reported at
   all, and nothing asks for a reason.

## Behavior

- **Tonnes.** The report carries every emission figure in metric tonnes to
  three decimals beside the kilograms, and the page presents the tonnes; the
  lines keep their kilograms for the arithmetic. Storage stays in kilograms.
- **Gas masses.** An HFC or PFC factor records the mass of gas per unit and
  the blend's GWP with its assessment report, in place of a CO2e-only
  component. Run lines and totals carry the mass for all seven gases, and the
  by-gas table shows mass and CO2e for each. The methodology statement names
  the inventory's assessment report and, when any factor's blend used
  another, names it and says that more than one report was used.
- **Cooling.** `PURCHASED_COOLING` joins the scope 2 categories; the
  declaration and the report label the scope 2 rows "electricity, steam, heat,
  and cooling".
- **Quality Criteria.** A market factor records `meetsQualityCriteria`
  (required) and `qualityNotes`. When false, the run does not apply the
  instrument: the facility's market-based figure falls back to the residual
  mix recorded for the inventory, or to the location-based figure when there
  is none, and the line's `marketNote` says why. The EMISSION_FACTOR gate
  warns about the fallback. The report lists every instrument with its
  assessment and notes, and the methodology counts the ones replaced.
- **Residual mix.** An inventory with any market factor records whether an
  adjusted residual mix is available and, when it is, its factor in kg CO2e
  per kWh. Until it does, the EMISSION_FACTOR gate warns. When none is
  available, the report prints the Guidance's sentence that an adjusted
  factor was not available and double counting between consumers may result;
  when one is, the report names the factor.
- **Method disclosure.** The report states that the inventory total uses the
  location-based scope 2 figure, states the base-year inventory's scope 2
  method, and states whether the base year's market-based figure is a
  location-based proxy (true when the base-year run had no instruments).
- **Excluded operations.** Every facility of the organization not in the
  boundary needs a **boundary exclusion** with a reason from the exclusion
  list (spec 04) and a detail: either for the facility itself, or for its
  whole entity, which covers every facility of it. An entity with no
  facilities emits nothing and needs none. The BOUNDARY gate errors on a
  facility that is neither in the boundary nor covered by an exclusion, so a
  run cannot be launched with an undocumented omission; freezing stays
  allowed, so the boundary can be drawn and the omissions reasoned about in
  either order. An exclusion is refused while the operation is in the
  boundary, and ticking an operation in retires the exclusion that covered
  it. The version copies the exclusions, a correction inherits them, and the
  report lists them under "Operations excluded from the boundary" before the
  record exclusions.

Given an inventory whose only instrument is a supplier certificate that fails
criterion 5 (sourced from another market): the accountant records
`meetsQualityCriteria` false with a note; the run reports that facility
market-based at the residual mix; the report names the instrument, the
criterion, and the fallback, and prints scope 2 in tonnes both ways.

## API

- `EmissionFactorResponse.gases` gains `hfcsKg` and `pfcsKg`, and the
  factor gains `blendGwpSource`; `RunResponse.byGas` and
  `RunLineResponse.byGas` gain `hfcsKg` and `pfcsKg`; a line gains
  `blendGwpSource` and `marketNote`.
- `ReportResponse.emissions` gains `scope1TCo2e`, `scope2LocationBasedTCo2e`,
  `scope2MarketBasedTCo2e`, `scope3TCo2e`, `totalTCo2e`, `totalMethod`
  (`LOCATION_BASED`), `baseYearScope2Method` (`LOCATION_BASED` or `DUAL`),
  `baseYearMarketBasedIsProxy`, `residualMixAvailable`,
  `residualMixKgCo2ePerKwh` and `residualMixDisclosure`; each `byGas` entry
  gains `tonnes` and `tCo2e`, and `kg` is never null; the report gains
  `biogenicCo2T` and `boundaryExclusions[]`; `methodology` gains
  `assessmentReports[]` and `multipleAssessmentReports`.
- `MarketFactorRequest` gains `meetsQualityCriteria` (required) and
  `qualityNotes?`; `PUT /inventories/{id}/residual-mix` `{available,
  kgCo2ePerKwh?}` (409 when available without a factor); `InventoryResponse`
  carries `residualMixAvailable` and `residualMixKgCo2ePerKwh`.
- `GET /inventories/{id}/boundary/exclusions`;
  `PUT /inventories/{id}/boundary/entities/{entityId}/exclude` and
  `PUT /inventories/{id}/boundary/{facilityId}/exclude` `{reason, detail?}` →
  the entity's `BoundaryEntityResponse`, which carries `exclusion` and an
  `exclusion` per facility; `DELETE` clears the exclusion. 409 unless the
  inventory is a draft, or when the operation is in the boundary.
  `BoundaryVersionResponse` gains `exclusions[]`.
- `ActivityCategory` gains `PURCHASED_COOLING`, and the library a district
  cooling factor.

## Data

`V19__required_disclosures.sql`: `hfcs_kg_per_unit`, `pfcs_kg_per_unit`, and
`blend_gwp_source` on `ghg_emission_factors`, with the seeded R-410A blend
restated as 1 kg of gas per kg at its IPCC AR5 potential; `hfcs_kg` and
`pfcs_kg` on runs and lines, `blend_gwp_source` and `market_note` on lines,
earlier R-410A lines backfilled; `meets_quality_criteria` (backfilled true)
and `quality_notes` ("Not assessed ... before spec 07.2") on
`ghg_market_factors`; `residual_mix_available` and
`residual_mix_kg_co2e_per_kwh` on `ghg_inventories`;
`ghg_boundary_exclusions` (inventory, entity or facility, reason, detail)
and `ghg_boundary_version_exclusions`; the category check widened to
`PURCHASED_COOLING` and a district cooling factor seeded.

## Events

None.

## Verification

`GhgApiIntegrationTests`: `hfcBlendsReportTheirMassAndTheAssessmentReportsUsed`
(mass, tonnes, purchased cooling, one report under AR5 and two under AR6),
`anInstrumentThatFailsTheQualityCriteriaFallsBackAndTheReportSaysWhy`,
`marketBasedScope2IsReportedBesideLocationBased` (the residual-mix warning
and disclosure, the total method), `structuralChangesAreFlaggedAgainstTheBaseYearThreshold`
(the base year's scope 2 method and proxy),
`anOperationLeftOutOfTheBoundaryNeedsAReasonAndTheReportListsIt`. Frontend:
`RunDetailPage.test.tsx` (tonnes, quality criteria, residual mix, assessment
reports, boundary exclusions), `InventoryDetailPage.test.tsx` (excluding a
facility). Manual: `docs/qa/003-inventory.md` section R.

## Non-goals and open questions

- The emission factor hierarchies of the Guidance (Tables 6.2 and 6.3) and
  certificate tracking or retirement records.
- A boundary exclusion is required for every facility of the organization,
  whichever entity holds it, because completeness of the boundary is what a
  verifier checks; an entity with no facilities needs none.
- An assurance statement and PDF export (spec 07 non-goals).
